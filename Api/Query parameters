Query Parameters in URLs

Query parameters are key-value pairs added to a URL after a question mark (?) to pass data to the server. Multiple parameters are separated by ampersands (&).

Example:
https://www.google.com/search?q=ajax+javascript

In this URL, the query parameter 'q' has the value 'ajax+javascript', which tells Google to search for "ajax javascript".  
